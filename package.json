{
  "name": "nikunj-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute portfolio-db --local --file=schema.sql",
    "db:seed": "wrangler d1 execute portfolio-db --local --file=data.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260717.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
